# Production extension build — copy to .env.production before `pnpm build`.
# The API URL origin is also used for connect-from-tab auth in the extension.

VITE_CURATOR_API_URL=https://your-curator-domain.com
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
